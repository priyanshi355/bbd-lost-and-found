{
  "name": "bbd-lost-found-monorepo",
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "frontend-build": "cd frontend && npm install && npm run build",
    "backend-start": "cd backend && npm install && npm start"
  }
}
